#!/bin/sh
installed_helper=/usr/lib/chatgpt-bin/updater/install_package.py
if [ -f "$installed_helper" ]; then
  exec /usr/bin/python "$installed_helper" "$@"
fi
script_dir=$(CDPATH= cd "$(dirname "$0")" && pwd) || exit 1
exec /usr/bin/python "$script_dir/install_package.py" "$@"
